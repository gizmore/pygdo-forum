# pygdo-forum

Fresh forum implementation for PyGDO.

The initial model is `GDO_ForumBoard`: a title and a `GDT_Tree` nested-set
interval (`board_tree_left`, `board_tree_right`). Higher-level concepts such
as threads, posts, subscriptions, permissions and chat commands will be added
only after their use cases are specified.


## Usage